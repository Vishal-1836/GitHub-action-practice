name: practice the secret\
on:
  pull_request:
    branches: ["main"]
  push:
    branches: ["main"]
  workflow_dispatch: 
jobs:
  buildJob:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: The secret print
        run: |
        
