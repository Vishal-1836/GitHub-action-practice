name: configuration-variable
on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: This is configuration variable
      run: |
        echo "This is configuration check workflow ${{ vars.project }}"
