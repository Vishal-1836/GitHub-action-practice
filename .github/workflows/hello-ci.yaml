name: 1-hello-ci
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main"]
  workflow_dispatch: 
  
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.0
      - name: This is task check value
        run: |
          echo "The event that trigger the worflow ${{ gitHub.event_name }}"
          echo "The branch name: ${{ github.ref_name }}"
          echo "The commit SHA ${{ github.sha }}"
          echo "The user who triggered the flow:  ${{ gitHub.actor }}"
