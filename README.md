# GitHub-action-practice
This repo is for the Github action practice
There are many type of variable:
env:
 ram: ram 
The above type of variable is use din workflow level, job level, step (task) level. But only limited to that workflow.

Then there are configuration variable which are repo variable vars.variablename

the context varible which are github.reponame, workflow_job_id, runner.os, runenr.os

